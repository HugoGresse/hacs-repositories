Search within HACS repositories, like you do with NPM. 

# Dev setup (to be completed)
1. Clone, install dependencies, also within `functions`
2. Add a function env with `github.token` and your GitHub API token

